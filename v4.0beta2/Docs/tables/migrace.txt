Migrace:

ze staré verze tabulky modulů:

ALTER TABLE `vypecky_modules` CHANGE `params` `mparams` VARCHAR( 100 ) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL

- změna názvu sloupce param na mparam, protože se param používá v item