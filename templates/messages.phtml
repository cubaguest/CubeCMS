<div class="message" id="infoMessages" style="
<?if(empty ($this->messages)) {?>
     display: none;
        <?}?>
     background-image: url('<?=vve_get_tpl_file('icons/accept.png', VVE_TPL_FILE_IMAGE)?>')">
     <?// Vypis hlasek
     if(!empty ($this->messages)) {
        foreach ($this->messages as $message) {
           print ("<p>".$message . "</p>");
        }
     }?>
   <hr class="reseter" />
   <script type="text/javascript">
      //setTimeout('document.getElementById("messages").style.display = "none"', 5000);
   </script>
</div>

{*-CORE_ERRORS-*}

<? // chyby modulu?>
<div class="message" id="errMessages" style="
    <?if(empty ($this->moduleErrors) AND Template::pVar('IE_WARN') == false) {?>
     display: none;
        <?}?>
     background-image: url('<?=vve_get_tpl_file('icons/error.png', VVE_TPL_FILE_IMAGE)?>')">
   <?
   if(Template::pVar('IE_WARN') == true){
      $this->includeTpl('ie6browser.phtml',true);
   }
   if(!empty ($this->moduleErrors)) {
      echo("<strong>".$this->tr("Chyba:")."</strong>");
      foreach ($this->moduleErrors as $error) {
         print ("<p>".$error ."</p>");
      }
   }?>
   <hr class="reseter" />
</div>
