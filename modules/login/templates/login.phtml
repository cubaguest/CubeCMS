<h1><?= $this->category()->getName(); ?></h1>
<div class="main-content-form-edit" id="login-form">
   <div class="clearfix">
      <?php
      $loginForm = Auth::getAuthenticator('internal')->getLoginForm();
      $loginForm->html()->addClass('clearfix');
      echo $loginForm->renderStart();
      ?>
         <div class="login-form-label">
            <h2><?= $this->tr('Přihlášení'); ?></h2>
         </div>
         <div>
            <label for="login-user"><?=$this->tr('Jméno / e-mail'); ?>:</label><br />
            <?=$loginForm->{Auth_Provider_Internal::FORM_USERNAME}->control()?>
         </div>
         <div>
            <label for="login-password"><?= $this->tr('Heslo'); ?>:</label><br />
            <?=$loginForm->{Auth_Provider_Internal::FORM_PASSWORD}->control()?>
         </div>
         <div class="left-box login-permanent">
            <?=$loginForm->{Auth_Provider_Internal::FORM_PERMANENT}->control()?>
            <label for="login-permanent"><?=$this->tr('trvale')?></label>
         </div>
         <div class="right-box">
            <?=$loginForm->{Auth_Provider_Internal::FORM_SUBMIT}->control()?>
         </div>
      <?php
      echo $loginForm->renderEnd();
      ?>
      <p class="login-lost-password">
         <a href="<?= $this->link()->route('newPassword') ?>" title="<?= $this->tr('Obnovit zapomenuté heslo') ?>"><?= $this->tr('Zapomenuté heslo') ?></a>
      </p>
      <?php if(Auth::isAuthenticator('google')){?>
      <div>
         <a href="<?=Auth::getAuthenticator('google')->getAuthUrl()?>"
            ><img width="200" src="https://developers.google.com/accounts/images/sign-in-with-google.png" alt="" /></a>
      </div>
      <?php }?>
      <?php if(Auth::isAuthenticator('openid')){?>
      <div>
         <form action="" method="post">
            <label for="">OpenID</label>
            <input type="text" name="<?=Auth_Provider_OpenID::FORM_ID_NAME?>" value="" />
            <button type="submit" name="<?=Auth_Provider_OpenID::FORM_SUBMIT_NAME?>">Přihlásit</button>
         </form>
      </div>
      <?php }?>
   </div>
</div>