<?
if($this->text != false AND (string)$this->text->{Text_Model_Detail::COLUMN_LABEL} != null) {
   $this->addPageTitle($this->text->{Text_Model_Detail::COLUMN_LABEL});
   $h1 = $this->text->{Text_Model_Detail::COLUMN_LABEL};
} else {
   $h1 = $this->category()->getName();
}
echo $this->toolbox;
?>
<h1><?$this->includeFile('tpl://engine:headline/icon.phtml'); echo $h1;?></h1>
<div class="main-content-detail">
   <div class="main-content-text"><?echo ($this->filter($this->text->{Text_Model_Detail::COLUMN_TEXT},'emoticons'));?></div>
   <?if($this->textPrivate != false AND (string)$this->textPrivate->{Text_Model_Detail::COLUMN_TEXT} != null){
      ?><div class="main-content-text"><?
      echo $this->textPrivate->{Text_Model_Detail::COLUMN_TEXT};
      echo $this->toolboxPrivate;
   ?></div><?
   }
   if($this->text != false){
         $this->includeTpl('articletools.phtml', true, array(
            'lastEditTime' => $this->text->{Text_Model::COLUMN_CHANGED_TIME},
//              'lastEditUser' => $this->article->{Model_Users::COLUMN_USERNAME},
              'linkToPdf' => $this->link()->route('detailExport', array('output' => 'pdf')),
              'linkToHtml' => $this->link()->route('detailExport', array('output' => 'html')),
              'shares' => array('url' => $this->link(), 'title' => $this->category()->getName())));
   }
   ?>
</div>
<?
if(($this->text != null AND strpos((string)$this->text->{Text_Model_Detail::COLUMN_TEXT}, 'pirobox') !== false)
   OR ($this->textPrivate != null AND strpos((string)$this->textPrivate->{Text_Model_Detail::COLUMN_TEXT}, 'pirobox') !== false)){
$this->addJsPlugin(new JsPlugin_PiroBox())?>
   <script type="text/javascript">
      /* <![CDATA[ */
      $(document).ready(function(){$().piroBox({slideShow : true,slideSpeed : 3});});
      /* ]]> */
</script>
<?}
?>
