<?
$h = $this->category()->getName();
if($this->text != false AND (string)$this->text->{Text_Model_Detail::COLUMN_LABEL} != null){
   $h = ($this->text->{Text_Model_Detail::COLUMN_LABEL});
   Template_Core::setPageTitle($h);
}
$this->addJsPlugin(new JsPlugin_JQuery());

echo $this->toolbox;
?>
<h1><?ps($h)?></h1>
<div class="content">
   <?if($this->text != false AND (string)$this->text->{Text_Model_Detail::COLUMN_TEXT} != null){?>
   <div class=""><?=$this->text->{Text_Model_Detail::COLUMN_TEXT}?></div>
   <?}?>
   <?
   $this->newMailForm->html()->setAttrib('id', 'registerNewMail');
   echo $this->newMailForm;
   ?>
      <br />
      <a href="<?=$this->link()->route('unregistration');?>"
         title="<?=$this->tr('Zrušit registraci e-mailu')?>"
         ><?=$this->tr('Zrušit registraci e-mailu')?></a>
</div>
<script type="text/javascript">
   /* <![CDATA[ */
   $('form#registerNewMail').submit(function(){
      $.ajax({type: "POST",
         url: "<?=$this->link()?>",
         data: $(this).serialize(),
         success: function(data){ vveShowMessages(data); }
      });
      return false;
   });
   /* ]]> */
</script>