<?
if($this->edit == true){
   if((string)$this->panel->{Model_Panel::COLUMN_NAME} != null){
      $h = sprintf($this->tr('Úprava panelu kategorie "%s"'), $this->panel->{Model_Panel::COLUMN_NAME} );
   } else {
      $h = sprintf($this->tr('Úprava panelu kategorie "%s"'), $this->panel->{Model_Category::COLUMN_NAME} );
   }
} else {
   $h = $this->tr('Nový panel');
}
?>
<h1><?ps($h);?></h1>
<div class="content-admin">
   <?
// donastavení formuláře
   $this->form->panel_order->html()->setAttrib('size', 4);
   $this->addJsPlugin(new JsPlugin_JQuery());
   echo ($this->form);
   ?>
</div>